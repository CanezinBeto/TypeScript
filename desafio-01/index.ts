// Função que recebe dois argumentos do tipo number
function sum(number_01: number, number_02: number) {
  return `A soma do número ${number_01} e ${number_02} é ${
    number_01 + number_02
  }`
}

console.log(sum(4, 1))
